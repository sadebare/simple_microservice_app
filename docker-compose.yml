version: "3.8"
services:
  server:
    build: ./server
    depends_on:
      - client
    ports:
      - 5000:5000
  client:
    build: ./client
    ports:
      - 80:80
    links:
      - server
    depends_on:
      - server
    restart: always 
